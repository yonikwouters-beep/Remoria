---
import BaseLayout from '../layouts/BaseLayout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
import FaqAccordion from '../components/FaqAccordion.astro';
import GuaranteeBadge from '../components/GuaranteeBadge.astro';
import { getLocale, t, tp, localizePath } from '../i18n/utils';

const locale = getLocale(Astro.currentLocale);

type HowStep = { title: string; text: string };
type FeatureCard = { title: string; sub: string; headline: string; body: string; points: string[] };
type PhotobookStep = { title: string; text: string };
type TrustItem = { title: string; text: string };
type FaqItem = { question: string; answer: string };

const howSteps = tp<HowStep[]>(locale, 'nabestaanden.how.steps');
const featureCards = tp<FeatureCard[]>(locale, 'nabestaanden.features.cards');
const photobookSteps = tp<PhotobookStep[]>(locale, 'nabestaanden.features.photobook.steps');
const reassureItems = tp<TrustItem[]>(locale, 'nabestaanden.features.reassure.items');
const trustItems = tp<TrustItem[]>(locale, 'nabestaanden.trust');
const nabestaandenFaqs = tp<FaqItem[]>(locale, 'nabestaanden.faqs.items');

const howIcons = [
  `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><line x1="19" y1="8" x2="19" y2="14"/><line x1="22" y1="11" x2="16" y2="11"/></svg>`,
  `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="18" cy="5" r="3"/><circle cx="6" cy="12" r="3"/><circle cx="18" cy="19" r="3"/><line x1="8.59" y1="13.51" x2="15.42" y2="17.49"/><line x1="15.41" y1="6.51" x2="8.59" y2="10.49"/></svg>`,
  `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/></svg>`,
];

const featureCardIcons = [
  `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21 15 16 10 5 21"/></svg>`,
  `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>`,
  `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/></svg>`,
];

/**
 * Screenshots worden bewust groter getoond dan hun podium en bloeden over de rand,
 * zodat de UI-tekst op leesbare schaal blijft in plaats van weggeschaald te worden.
 */
const featureShowcase = [
  { img: '/mediacollectie-preview.png', width: '114%', top: '-2%', left: '-7%' },
  { img: '/community-chat-preview.png', width: '142%', top: '0%', left: '0%' },
  { img: '/eerbetonen-preview.png', width: '146%', top: '-7%', left: '-1%' },
];

const reassureIcons = [
  `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/><path d="M9 12l2 2 4-4"/></svg>`,
  `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>`,
  `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12a9 9 0 1 1-3.5-7.1"/><polyline points="21 3 21 9 15 9"/><polyline points="12 7 12 12 15.5 14"/></svg>`,
];

const photobookStepIcons = [
  `<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21 15 16 10 5 21"/></svg>`,
  `<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>`,
  `<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 12V22H4V12"/><path d="M22 7H2v5h20V7z"/><path d="M12 22V7"/><path d="M12 7H7.5a2.5 2.5 0 0 1 0-5C11 2 12 7 12 7z"/><path d="M12 7h4.5a2.5 2.5 0 0 0 0-5C13 2 12 7 12 7z"/></svg>`,
];

const trustIcons = [
  `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>`,
  `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>`,
  `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/></svg>`,
  `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/></svg>`,
];

const prijzenUrl = localizePath(locale, '/prijzen');
---

<BaseLayout title={t(locale, 'nabestaanden.meta.title')} description={t(locale, 'nabestaanden.meta.description')}>
  <Navbar />

  <!-- ══════════════════════════════════════════════
       1. HERO
  ══════════════════════════════════════════════ -->
  <section class="hero" aria-labelledby="hero-heading" data-reveal>
    <div class="hero__overlay" aria-hidden="true"></div>
    <div class="container">
      <div class="hero__content" data-reveal>
        <p class="eyebrow">{t(locale, 'nabestaanden.hero.eyebrow')}</p>
        <h1 class="hero__title" id="hero-heading">
          {t(locale, 'nabestaanden.hero.titleLine1')}<br />{t(locale, 'nabestaanden.hero.titleLine2')}<br />{t(locale, 'nabestaanden.hero.titleLine3')}
        </h1>
        <p class="hero__body">
          {t(locale, 'nabestaanden.hero.body')}
        </p>
        <div class="hero__actions">
          <a href={prijzenUrl} class="btn btn--primary">
            {t(locale, 'nabestaanden.hero.ctaPrimary')}
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M5 12h14M12 5l7 7-7 7"/>
            </svg>
          </a>
          <a
            href="https://remoria.eu/demo"
            class="btn btn--ghost"
            target="_blank"
            rel="noopener noreferrer"
          >
            <span class="btn__play" aria-hidden="true">
              <svg width="9" height="11" viewBox="0 0 9 11" fill="currentColor">
                <path d="M0 0l9 5.5L0 11V0z"/>
              </svg>
            </span>
            {t(locale, 'nabestaanden.hero.ctaSecondary')}
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════════
       2. HOW IT WORKS
  ══════════════════════════════════════════════ -->
  <section class="how" id="hoe-werkt-het">
    <div class="container">

      <div class="how__header" data-reveal>
        <div class="how__divider" aria-hidden="true"></div>
        <p class="eyebrow eyebrow--center">{t(locale, 'nabestaanden.how.eyebrow')}</p>
        <h2 class="section-title">{t(locale, 'nabestaanden.how.title').split('\n').map((line, i, arr) => i < arr.length - 1 ? [line, <br />] : line)}</h2>
        <p class="section-sub">{t(locale, 'nabestaanden.how.sub')}</p>
      </div>

      <div class="how__cards">
        {howSteps.map((step, i) => (
          <div class="how-card" data-reveal data-reveal-delay={String(i * 80)}>
            <div class="how-card__top">
              <div class="how-card__icon" aria-hidden="true" set:html={howIcons[i]} />
              <span class="how-card__step">{i + 1}</span>
            </div>
            <h3 class="how-card__title">{step.title}</h3>
            <p class="how-card__text">{step.text}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════════
       3. FEATURES GRID
  ══════════════════════════════════════════════ -->
  <section class="fg">
    <div class="container">

      <!-- Header -->
      <div class="fg__header" data-reveal>
        <p class="eyebrow eyebrow--center">
          <span class="eyebrow__plus" aria-hidden="true">+</span> {t(locale, 'nabestaanden.features.eyebrow')}
        </p>
        <h2 class="section-title fg__title">
          {t(locale, 'nabestaanden.features.title')}
        </h2>
        <p class="section-sub fg__sub">
          {t(locale, 'nabestaanden.features.sub')}
        </p>
      </div>

      <!-- Showcase rows -->
      <div class="fg__rows">
        {featureCards.map((card, i) => (
          <article class="fg-row" data-reveal>
            <div class="fg-row__copy">
              <p class="fg-row__label">
                <span class="fg-row__label-icon" aria-hidden="true" set:html={featureCardIcons[i]} />
                {card.title}
              </p>
              <h3 class="fg-row__title">{card.headline}</h3>
              <p class="fg-row__text">{card.body}</p>
              <ul class="fg-row__points">
                {card.points.map((point) => (
                  <li class="fg-row__point">
                    <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                      <polyline points="20 6 9 17 4 12"/>
                    </svg>
                    <span>{point}</span>
                  </li>
                ))}
              </ul>
            </div>

            <div class={`fg-row__stage fg-row__stage--${i + 1}`}>
              <div class="fg-shot">
                <img
                  src={featureShowcase[i].img}
                  alt={card.title}
                  class="fg-shot__img"
                  style={`width:${featureShowcase[i].width};top:${featureShowcase[i].top};left:${featureShowcase[i].left};`}
                  loading="lazy"
                  decoding="async"
                />
              </div>
            </div>
          </article>
        ))}

        <!-- Photobook row -->
        <article class="fg-row fg-row--book" id="fotoboek" data-reveal>
          <div class="fg-row__copy">
            <p class="fg-row__label">
              <span class="fg-row__label-icon" aria-hidden="true">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/>
                  <path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/>
                </svg>
              </span>
              {t(locale, 'nabestaanden.features.photobook.title')}
            </p>
            <h3 class="fg-row__title">{t(locale, 'nabestaanden.features.photobook.headline')}</h3>
            <p class="fg-row__text">{t(locale, 'nabestaanden.features.photobook.body')}</p>

            <ul class="fg-steps">
              {photobookSteps.map((step, i) => (
                <li class="fg-step">
                  <div class="fg-step__icon" aria-hidden="true" set:html={photobookStepIcons[i]} />
                  <div>
                    <p class="fg-step__title">{step.title}</p>
                    <p class="fg-step__text">{step.text}</p>
                  </div>
                </li>
              ))}
            </ul>
          </div>

          <div class="fg-row__stage fg-row__stage--book">
            <img
              src="/fotoboek-preview.png"
              alt={t(locale, 'nabestaanden.features.photobook.title')}
              class="fg-row__photo"
              loading="lazy"
              decoding="async"
            />
          </div>
        </article>
      </div>

      <!-- Reassurance -->
      <div class="fg-reassure" data-reveal>
        <p class="fg-reassure__title">{t(locale, 'nabestaanden.features.reassure.title')}</p>
        <div class="fg-reassure__grid">
          {reassureItems.map((item, i) => (
            <div class="fg-reassure__item">
              <div class="fg-reassure__icon" aria-hidden="true" set:html={reassureIcons[i]} />
              <div>
                <p class="fg-reassure__item-title">{item.title}</p>
                <p class="fg-reassure__item-text">{item.text}</p>
              </div>
            </div>
          ))}
        </div>
      </div>

    </div>
  </section>

  <!-- ══════════════════════════════════════════════
       4. QUOTE BANNER
  ══════════════════════════════════════════════ -->
  <section class="quote-banner">
    <div class="container">
      <div class="quote-banner__inner" data-reveal>
        <div class="quote-banner__overlay"></div>
        <div class="quote-banner__content">
          <p class="quote-banner__eyebrow">{t(locale, 'nabestaanden.quoteEyebrow')}</p>
          <blockquote class="quote-banner__quote">
            {t(locale, 'nabestaanden.quoteLine1')}<br />{t(locale, 'nabestaanden.quoteLine2')}
          </blockquote>
          <span class="quote-banner__rule" aria-hidden="true"></span>
          <p class="quote-banner__sub">{t(locale, 'nabestaanden.quoteSub')}</p>
          <div class="quote-banner__actions">
            <a href={prijzenUrl} class="btn btn--gold">{t(locale, 'nabestaanden.quoteCta')}</a>
            <span class="quote-banner__note">{t(locale, 'nabestaanden.quoteCtaNote')}</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════════
       5. FINAL CTA
  ══════════════════════════════════════════════ -->
  <section class="cta" data-reveal>
    <div class="container">
      <div class="cta__inner">
        <h2 class="cta__title">{t(locale, 'nabestaanden.cta.titleLine1')}<br />{t(locale, 'nabestaanden.cta.titleLine2')}</h2>
        <p class="cta__sub">
          {t(locale, 'nabestaanden.cta.sub')}
        </p>
        <a href={prijzenUrl} class="btn btn--primary btn--lg">
          {t(locale, 'nabestaanden.cta.button')}
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </a>
        <div class="cta__guarantee">
          <GuaranteeBadge />
        </div>
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════════
       6. TRUST BAR
  ══════════════════════════════════════════════ -->
  <section class="trust" data-reveal>
    <div class="container">
      <div class="trust__grid">
        {trustItems.map((item, i) => (
          <div class="trust-item">
            <div class="trust-item__icon" aria-hidden="true" set:html={trustIcons[i]} />
            <h4 class="trust-item__title">{item.title}</h4>
            <p class="trust-item__text">{item.text}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <FaqAccordion title={t(locale, 'nabestaanden.faqs.title')} items={nabestaandenFaqs} id="nabestaanden-faq" />

  <Footer />
</BaseLayout>

<!-- ═══════════════════════════════════════════════════════════
     SCROLL REVEAL
═══════════════════════════════════════════════════════════ -->
<script>
  (function () {
    const els = document.querySelectorAll('[data-reveal]');
    if (!els.length) return;

    const io = new IntersectionObserver(
      (entries) => {
        entries.forEach((e) => {
          if (e.isIntersecting) {
            const delay = (e.target as HTMLElement).dataset.revealDelay || '0';
            setTimeout(() => e.target.classList.add('is-visible'), Number(delay));
            io.unobserve(e.target);
          }
        });
      },
      { threshold: 0.12 }
    );

    els.forEach((el) => io.observe(el));
  })();
</script>

<style>
  /* ─── Tokens ──────────────────────────────────── */
  :root {
    --bg:        #F7F4EE;
    --ink:       #1E293B;
    --muted:     #6B7280;
    --gold:      #C6A24B;
    --gold-soft: rgba(198, 162, 75, 0.12);
    --card-bg:   rgba(255, 255, 255, 0.72);
    --radius-img: 28px;
    --radius-card: 20px;
  }

  /* ─── Base ─────────────────────────────────────── */
  section {
    background-color: var(--bg);
  }

  .container {
    max-width: 1160px;
    margin: 0 auto;
    padding-inline: clamp(20px, 5vw, 56px);
  }

  /* ─── Reveal ───────────────────────────────────── */
  [data-reveal] {
    opacity: 0;
    transform: translateY(28px);
    transition: opacity 0.72s cubic-bezier(0.22, 1, 0.36, 1),
                transform 0.72s cubic-bezier(0.22, 1, 0.36, 1);
  }
  [data-reveal].is-visible {
    opacity: 1;
    transform: none;
  }

  /* ─── Typography ────────────────────────────────── */
  .eyebrow {
    display: inline-block;
    font-family: 'Inter', sans-serif;
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 18px;
  }
  .eyebrow--center { display: block; text-align: center; }

  .section-title {
    font-family: 'Playfair Display', Georgia, serif;
    font-size: clamp(1.9rem, 3.2vw, 2.85rem);
    font-weight: 500;
    line-height: 1.12;
    letter-spacing: -0.02em;
    color: var(--ink);
    margin-bottom: 16px;
  }

  .section-sub {
    font-family: 'Inter', sans-serif;
    font-size: clamp(15px, 1.4vw, 16px);
    line-height: 1.75;
    color: var(--muted);
  }

  /* ─── Buttons ───────────────────────────────────── */
  .btn {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 14px 28px;
    border-radius: 999px;
    font-family: 'Inter', sans-serif;
    font-size: 15px;
    font-weight: 500;
    text-decoration: none;
    transition: transform 0.2s ease, box-shadow 0.2s ease, background-color 0.2s ease;
    cursor: pointer;
    border: none;
  }
  .btn:hover { transform: translateY(-2px); }

  .btn--primary {
    background: var(--ink);
    color: #fff;
    box-shadow: 0 4px 20px rgba(30, 41, 59, 0.2);
  }
  .btn--primary:hover {
    background: #0f172a;
    box-shadow: 0 8px 28px rgba(30, 41, 59, 0.28);
  }

  .btn--ghost {
    background: #fff;
    color: var(--ink);
    border: 1px solid rgba(30, 41, 59, 0.14);
    box-shadow: 0 1px 4px rgba(30, 41, 59, 0.06);
  }
  .btn--ghost:hover {
    border-color: rgba(30, 41, 59, 0.24);
    box-shadow: 0 4px 16px rgba(30, 41, 59, 0.1);
  }

  .btn--lg { padding: 16px 36px; font-size: 16px; }

  .btn__play {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 28px;
    height: 28px;
    background: var(--ink);
    color: #fff;
    border-radius: 50%;
    flex-shrink: 0;
  }

  /* ════════════════════════════════════════════
     HERO
  ════════════════════════════════════════════ */
  .hero {
    position: relative;
    background-color: var(--bg);
    background-image: url('/Voor-nabestaanden-web.webp');
    background-size: cover;
    background-position: 66% 18%;
    background-repeat: no-repeat;
    min-height: clamp(520px, 64vw, 740px);
    display: flex;
    align-items: center;
    overflow: hidden;
  }

  .hero__overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to right,
      #F7F4EE 0%,
      rgba(247, 244, 238, 0.97) 26%,
      rgba(247, 244, 238, 0.82) 44%,
      rgba(247, 244, 238, 0.45) 62%,
      rgba(247, 244, 238, 0.08) 82%,
      rgba(247, 244, 238, 0) 100%
    );
  }

  .hero .container {
    position: relative;
    z-index: 1;
    width: 100%;
    padding-block: clamp(64px, 9vw, 104px);
  }

  .hero__content {
    max-width: 520px;
  }

  .hero__title {
    font-family: 'Playfair Display', Georgia, serif;
    font-size: clamp(2.2rem, 4.2vw, 3.8rem);
    font-weight: 500;
    line-height: 1.08;
    letter-spacing: -0.025em;
    color: var(--ink);
    margin-bottom: 24px;
  }

  .hero__body {
    font-family: 'Inter', sans-serif;
    font-size: clamp(15px, 1.5vw, 16px);
    line-height: 1.8;
    color: var(--muted);
    max-width: 460px;
    margin-bottom: 40px;
  }

  .hero__actions {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 14px;
  }

  /* ════════════════════════════════════════════
     HOW IT WORKS
  ════════════════════════════════════════════ */
  .how {
    padding: clamp(72px, 10vh, 112px) 0;
    scroll-margin-top: 24px;
  }

  .how__header {
    text-align: center;
    max-width: 540px;
    margin: 0 auto clamp(52px, 7vh, 80px);
  }

  .how__divider {
    width: 36px;
    height: 2px;
    background: var(--gold);
    margin: 0 auto 20px;
    border-radius: 2px;
  }

  .how__cards {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: clamp(20px, 2.5vw, 32px);
  }

  .how-card {
    background: var(--card-bg);
    border: 1px solid rgba(30, 41, 59, 0.07);
    border-radius: var(--radius-card);
    padding: clamp(28px, 3.5vw, 44px) clamp(24px, 3vw, 38px);
    box-shadow:
      0 1px 3px rgba(30, 41, 59, 0.05),
      0 12px 32px rgba(30, 41, 59, 0.07);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }
  .how-card:hover {
    transform: translateY(-4px);
    box-shadow:
      0 4px 8px rgba(30, 41, 59, 0.06),
      0 20px 48px rgba(30, 41, 59, 0.11);
  }

  .how-card__top {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    margin-bottom: 24px;
  }

  .how-card__icon {
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--gold-soft);
    border-radius: 14px;
    color: var(--gold);
  }

  .how-card__step {
    font-family: 'Playfair Display', Georgia, serif;
    font-size: 2.2rem;
    font-weight: 500;
    color: rgba(198, 162, 75, 0.18);
    line-height: 1;
  }

  .how-card__title {
    font-family: 'Playfair Display', Georgia, serif;
    font-size: clamp(1.05rem, 1.4vw, 1.2rem);
    font-weight: 500;
    color: var(--ink);
    line-height: 1.35;
    margin-bottom: 14px;
  }

  .how-card__text {
    font-family: 'Inter', sans-serif;
    font-size: 14.5px;
    line-height: 1.75;
    color: var(--muted);
  }

  /* ════════════════════════════════════════════
     FEATURES GRID
  ════════════════════════════════════════════ */
  .fg {
    position: relative;
    padding: clamp(72px, 10vh, 112px) 0 clamp(40px, 5vh, 60px);
  }

  /* Zachte warme wash zodat de sectie niet als één vlak wit leest */
  .fg::before {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
      radial-gradient(58% 30% at 82% 16%, rgba(198, 162, 75, 0.11) 0%, transparent 70%),
      radial-gradient(52% 28% at 12% 52%, rgba(122, 141, 122, 0.1) 0%, transparent 70%),
      radial-gradient(60% 32% at 88% 86%, rgba(198, 162, 75, 0.09) 0%, transparent 70%);
  }

  .fg > .container {
    position: relative;
    z-index: 1;
  }

  .fg__header {
    text-align: center;
    max-width: 700px;
    margin: 0 auto clamp(44px, 6vh, 64px);
  }

  .eyebrow__plus {
    color: var(--gold);
    font-size: 13px;
    margin-right: 2px;
  }

  .fg__title {
    text-align: center;
    margin-bottom: 18px;
  }

  .fg__sub {
    text-align: center;
  }

  /* ── Showcase rows ── */
  .fg__rows {
    display: flex;
    flex-direction: column;
    gap: clamp(40px, 6vh, 76px);
  }

  .fg-row {
    display: grid;
    grid-template-columns: 1fr 1.32fr;
    align-items: center;
    gap: clamp(28px, 3.6vw, 56px);
    scroll-margin-top: 24px;
  }

  .fg-row:nth-child(even) .fg-row__copy {
    order: 2;
  }

  .fg-row__copy {
    max-width: 460px;
  }

  .fg-row__label {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    font-family: 'Inter', sans-serif;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 18px;
  }

  .fg-row__label-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 34px;
    height: 34px;
    background: var(--gold-soft);
    border-radius: 10px;
    color: var(--gold);
  }

  .fg-row__title {
    font-family: 'Playfair Display', Georgia, serif;
    font-size: clamp(1.5rem, 2.5vw, 2.05rem);
    font-weight: 500;
    line-height: 1.2;
    letter-spacing: -0.015em;
    color: var(--ink);
    white-space: pre-line;
    margin-bottom: 16px;
  }

  .fg-row__text {
    font-family: 'Inter', sans-serif;
    font-size: 15.5px;
    line-height: 1.8;
    color: var(--muted);
  }

  .fg-row__points {
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 24px;
  }

  .fg-row__point {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 15px 8px 12px;
    background: rgba(255, 255, 255, 0.72);
    border: 1px solid rgba(30, 41, 59, 0.08);
    border-radius: 999px;
    font-family: 'Inter', sans-serif;
    font-size: 13px;
    color: var(--ink);
    box-shadow: 0 1px 3px rgba(30, 41, 59, 0.05);
  }

  .fg-row__point svg {
    color: var(--gold);
    flex-shrink: 0;
  }

  /* Tinted stage carrying the screenshot */
  .fg-row__stage {
    position: relative;
    border-radius: 24px;
    padding: clamp(16px, 1.9vw, 24px);
    overflow: hidden;
    box-shadow:
      0 1px 2px rgba(30, 41, 59, 0.05),
      0 18px 44px rgba(30, 41, 59, 0.1);
  }

  .fg-row__stage::after {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: 24px;
    border: 1px solid rgba(255, 255, 255, 0.5);
    pointer-events: none;
  }

  .fg-row__stage--1 {
    background: linear-gradient(150deg, #f5ecdc 0%, #e7d9c1 100%);
  }
  .fg-row__stage--2 {
    background: linear-gradient(150deg, #e9eee9 0%, #d6e0d7 100%);
  }
  .fg-row__stage--3 {
    background: linear-gradient(150deg, #f3ece7 0%, #e4d5cb 100%);
  }
  .fg-row__stage--book {
    background: linear-gradient(150deg, #e7ece4 0%, #d3ddd2 100%);
    display: flex;
    padding: 0;
    min-height: clamp(340px, 40vw, 520px);
  }

  .fg-shot {
    position: relative;
    aspect-ratio: 4 / 3;
    border-radius: 12px;
    overflow: hidden;
    background: #fff;
    box-shadow:
      0 2px 6px rgba(30, 41, 59, 0.08),
      0 22px 46px rgba(30, 41, 59, 0.22);
  }

  .fg-shot__img {
    position: absolute;
    max-width: none;
    display: block;
  }

  .fg-row__photo {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    display: block;
    border-radius: 24px;
  }

  .fg-steps {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 18px;
    margin-top: 26px;
  }

  .fg-step {
    display: flex;
    align-items: flex-start;
    gap: 14px;
  }

  .fg-step__icon {
    flex-shrink: 0;
    width: 34px;
    height: 34px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--gold-soft);
    border-radius: 9px;
    color: var(--gold);
    margin-top: 1px;
  }

  .fg-step__title {
    font-family: 'Inter', sans-serif;
    font-size: 14px;
    font-weight: 600;
    color: var(--ink);
    margin-bottom: 3px;
    line-height: 1.3;
  }

  .fg-step__text {
    font-family: 'Inter', sans-serif;
    font-size: 13.5px;
    line-height: 1.65;
    color: var(--muted);
  }

  /* ── Reassurance strip ── */
  .fg-reassure {
    margin-top: clamp(48px, 7vh, 84px);
    background: linear-gradient(140deg, rgba(255, 255, 255, 0.92) 0%, rgba(250, 245, 236, 0.92) 100%);
    border: 1px solid rgba(198, 162, 75, 0.24);
    border-radius: 22px;
    padding: clamp(28px, 3.4vw, 40px);
    box-shadow: 0 2px 20px rgba(30, 41, 59, 0.06);
  }

  .fg-reassure__title {
    font-family: 'Playfair Display', Georgia, serif;
    font-size: clamp(1.05rem, 1.5vw, 1.25rem);
    font-weight: 500;
    color: var(--ink);
    text-align: center;
    margin-bottom: clamp(22px, 3vh, 30px);
  }

  .fg-reassure__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: clamp(20px, 3vw, 36px);
  }

  .fg-reassure__item {
    display: flex;
    align-items: flex-start;
    gap: 14px;
  }

  .fg-reassure__icon {
    flex-shrink: 0;
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--gold-soft);
    border-radius: 11px;
    color: var(--gold);
  }

  .fg-reassure__item-title {
    font-family: 'Inter', sans-serif;
    font-size: 14.5px;
    font-weight: 600;
    color: var(--ink);
    line-height: 1.35;
    margin-bottom: 5px;
  }

  .fg-reassure__item-text {
    font-family: 'Inter', sans-serif;
    font-size: 13.5px;
    line-height: 1.7;
    color: var(--muted);
  }

  /* ── Responsive ── */
  @media (max-width: 900px) {
    .fg-row,
    .fg-row--book {
      grid-template-columns: 1fr;
    }

    .fg-row:nth-child(even) .fg-row__copy {
      order: 0;
    }

    .fg-row__copy {
      max-width: none;
    }

    .fg-row__stage--book {
      min-height: 300px;
    }

    .fg-reassure__grid {
      grid-template-columns: 1fr;
      gap: 20px;
    }
  }

  @media (max-width: 560px) {
    .fg-row__stage {
      padding: 14px;
      border-radius: 18px;
    }

    .fg-shot {
      aspect-ratio: 1 / 1;
    }
  }

  /* ════════════════════════════════════════════
     QUOTE BANNER
  ════════════════════════════════════════════ */
  .quote-banner {
    padding: clamp(32px, 4vh, 48px) 0;
  }

  .quote-banner__inner {
    position: relative;
    border-radius: var(--radius-img);
    overflow: hidden;
    min-height: clamp(360px, 44vw, 520px);
    display: flex;
    align-items: center;
    justify-content: center;
    background-image: url('/banner-voor-nabestaanden.webp');
    background-size: cover;
    background-position: center 35%;
  }

  .quote-banner__overlay {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(70% 90% at 50% 45%, rgba(15, 23, 42, 0.5) 0%, rgba(15, 23, 42, 0.86) 100%),
      linear-gradient(135deg, rgba(15, 23, 42, 0.62) 0%, rgba(46, 38, 28, 0.62) 100%);
  }

  .quote-banner__content {
    position: relative;
    z-index: 2;
    text-align: center;
    max-width: 720px;
    padding: clamp(44px, 6vw, 76px) clamp(24px, 6vw, 64px);
  }

  .quote-banner__eyebrow {
    font-family: 'Inter', sans-serif;
    font-size: 11.5px;
    font-weight: 600;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 20px;
  }

  .quote-banner__quote {
    font-family: 'Playfair Display', Georgia, serif;
    font-size: clamp(1.5rem, 3.2vw, 2.35rem);
    font-weight: 500;
    line-height: 1.38;
    letter-spacing: -0.015em;
    color: #fff;
    font-style: normal;
  }

  .quote-banner__rule {
    display: block;
    width: 54px;
    height: 1px;
    margin: clamp(22px, 3vh, 30px) auto;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
  }

  .quote-banner__sub {
    font-family: 'Inter', sans-serif;
    font-size: clamp(14.5px, 1.4vw, 15.5px);
    line-height: 1.8;
    color: rgba(255, 255, 255, 0.78);
  }

  .quote-banner__actions {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 12px;
    margin-top: clamp(26px, 3.5vh, 34px);
  }

  .btn--gold {
    background: var(--gold);
    color: #1E293B;
    font-weight: 600;
    box-shadow: 0 6px 24px rgba(198, 162, 75, 0.34);
  }
  .btn--gold:hover {
    background: #d4b063;
    box-shadow: 0 10px 32px rgba(198, 162, 75, 0.44);
  }

  .quote-banner__note {
    font-family: 'Inter', sans-serif;
    font-size: 12.5px;
    color: rgba(255, 255, 255, 0.62);
  }

  /* ════════════════════════════════════════════
     CTA
  ════════════════════════════════════════════ */
  .cta {
    padding: clamp(80px, 11vh, 128px) 0;
  }

  .cta__inner {
    text-align: center;
    max-width: 560px;
    margin: 0 auto;
  }

  .cta__title {
    font-family: 'Playfair Display', Georgia, serif;
    font-size: clamp(2rem, 3.4vw, 3rem);
    font-weight: 500;
    line-height: 1.12;
    letter-spacing: -0.02em;
    color: var(--ink);
    margin-bottom: 18px;
  }

  .cta__sub {
    font-family: 'Inter', sans-serif;
    font-size: clamp(15px, 1.4vw, 16px);
    line-height: 1.75;
    color: var(--muted);
    margin-bottom: 36px;
  }

  .cta__guarantee {
    display: flex;
    justify-content: center;
    margin-top: 22px;
  }

  /* ════════════════════════════════════════════
     TRUST BAR
  ════════════════════════════════════════════ */
  .trust {
    padding: clamp(40px, 5vh, 60px) 0 clamp(72px, 9vh, 100px);
  }

  .trust__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: clamp(16px, 2vw, 24px);
    background: rgba(255, 255, 255, 0.52);
    border: 1px solid rgba(30, 41, 59, 0.07);
    border-radius: 20px;
    padding: clamp(28px, 3.5vw, 40px) clamp(20px, 3vw, 36px);
    box-shadow: 0 2px 16px rgba(30, 41, 59, 0.06);
  }

  .trust-item {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 10px;
  }

  .trust-item__icon {
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--gold-soft);
    border-radius: 11px;
    color: var(--gold);
    flex-shrink: 0;
  }

  .trust-item__title {
    font-family: 'Inter', sans-serif;
    font-size: 14px;
    font-weight: 600;
    color: var(--ink);
    line-height: 1.3;
  }

  .trust-item__text {
    font-family: 'Inter', sans-serif;
    font-size: 13.5px;
    line-height: 1.6;
    color: var(--muted);
  }

  /* ════════════════════════════════════════════
     RESPONSIVE
  ════════════════════════════════════════════ */
  @media (max-width: 960px) {
    .how__cards {
      grid-template-columns: 1fr;
      max-width: 520px;
      margin: 0 auto;
    }

    .trust__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 860px) {
    .hero {
      background-size: cover;
      background-position: 70% 15%;
      min-height: clamp(440px, 70vw, 580px);
    }

    .hero__overlay {
      background: linear-gradient(
        to right,
        #F7F4EE 0%,
        rgba(247, 244, 238, 0.98) 38%,
        rgba(247, 244, 238, 0.88) 58%,
        rgba(247, 244, 238, 0.35) 78%,
        rgba(247, 244, 238, 0) 100%
      );
    }

    .hero__content {
      max-width: 100%;
    }
  }

  @media (max-width: 720px) {
    .hero__actions {
      flex-direction: column;
      align-items: stretch;
    }

    .btn {
      justify-content: center;
    }

    .trust__grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  @media (max-width: 600px) {
    .hero {
      background-size: cover;
      background-position: 78% 12%;
    }

    .hero__overlay {
      background: linear-gradient(
        to right,
        #F7F4EE 0%,
        rgba(247, 244, 238, 0.98) 52%,
        rgba(247, 244, 238, 0.75) 72%,
        rgba(247, 244, 238, 0.2) 92%,
        rgba(247, 244, 238, 0) 100%
      );
    }
  }

  @media (max-width: 480px) {
    .trust__grid {
      grid-template-columns: 1fr;
    }
  }
</style>
